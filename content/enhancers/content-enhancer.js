/**
 * @callback ContentEnhancer
 * @param { Element } container
 * @returns { void | Promise<void> }
 */

/**
 * ContentEnhancer contract.
 *
 * An enhancer is an idempotent function accepting an injected Element and
 * returning void or Promise<void>.
 */
