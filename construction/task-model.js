/**
 * Adapter/query boundary over construction-task DOM nodes.
 *
 * Planned exports include direct tasks, task lookup, row/title/description,
 * authored lesson, ancestors, siblings, status wording, trail text, and counts.
 * All queries receive their root explicitly; none read the content host.
 */
