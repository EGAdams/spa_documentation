# spa_documentation

Before doing any work in this directory, invoke the Skill tool with
`skill: building-the-spa` first — it has the nav-drill-down rules, directory
layout, and file-naming conventions this SPA depends on. Load it every time,
not just when it seems relevant; documentation work here is the only thing
happening in this directory.
