/**
 * Composite operations over the documentation catalog.
 *
 * Planned exports:
 * - resolveNode( top, path )
 * - hasChildren( node )
 * - filePath( itemKey, file )
 * - itemKeyOf( top, path )
 */
