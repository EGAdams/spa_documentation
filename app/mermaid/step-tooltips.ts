import type { DiagramInteraction } from "./diagram-interaction.js";

/** Owns only positional sequence-number tooltip behavior. */
export declare function createStepTooltipInteraction(
    document: Document,
): DiagramInteraction;
