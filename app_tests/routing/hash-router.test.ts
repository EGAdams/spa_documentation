/**
 * Test-design stub for app/routing/hash-router.ts.
 *
 * Sole test responsibility: browser hash-event adapter.
 * Planned evidence:
 * - applies the initial route and later hash changes through injected commands.
 * - writes routes without feedback loops and removes listeners on disposal.
 *
 * No executable tests are implemented in this scaffold.
 */
export {};
